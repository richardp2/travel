image: ruby:2.3

pages:
  stage: deploy
  script:
  - gem uninstall -a -q jekyll
  - gem uninstall -a -q github-pages
  - bundle install
  - bundle exec jekyll build -d public --baseurl /travel
  artifacts:
    paths:
    - public
  only:
  - pages
